#!/usr/bin/env ruby

require_relative "../lib/items"

require 'trollop'

opts = Trollop::options do
  opt :root, "Folder containing radars",  :default => "radars"  
end

def run_command opts
  items = Items.new (opts[:root])

  items.with_recs_json
end

puts run_command(opts).join("\n,")
